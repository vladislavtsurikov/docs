---
title: "Precise Place"
description: "Place one object at a time with live preview, overlap checks, mouse actions, and per-prototype offsets"
---

Precise Place is a Happy Artist tool for intentional manual placement.

It gives you a live preview object, lets you drag and reposition before confirming, and supports extra mouse-action tools for rotation, scale, and directional movement.

<Info>
  New to Precise Place? Follow the step-by-step [Precise Place — Getting Started](/megaworld/getting-started/precise-place) tutorial first.
</Info>

## Supported resource types

Precise Place supports:

- [Terrain Object](/megaworld/resource-types/terrain-object)
- [Unity/GameObject](/megaworld/resource-types/unity-gameobject)

## Tool-level controls

Precise Place uses **Precise Place Tool Settings**.

The confirmed settings include:

- [Spacing](/megaworld/shared-settings/spacing) — minimum distance between consecutive placements. Default is `5`.
- **Random Select Prototype** — picks a random prototype from the active group on each placement instead of using the selected one. Enabled by default.
- **Remember Past Transform** — when enabled, the next placement reuses the transform (position, rotation, scale) from the previous one as a starting point.
- **Use Transform Components** — applies the [Transform Components](/megaworld/transform-components/overview) stack to each placed object. Enabled by default.
- **Overlap Check** — enables overlap rejection using [Overlap Check Settings](/megaworld/overlap-check-settings/overview).
- **Visualize Overlap Check Settings** — draws the overlap shapes in the scene view.
- **Enable Snap Move** — enables grid-snapping during drag repositioning.
- **Snap Move** — the grid step size for each axis (X, Y, Z) used when Snap Move is enabled. Default is `(2.5, 2.5, 2.5)`.
- **Align** — aligns the placed object to the surface normal.
- **Weight To Normal** — blends between world-up and surface normal for the alignment. At `1` the object fully aligns to the surface normal.
- **Along Stroke** — rotates the object to face the direction of the stroke.

## Prototype-level controls

Precise Place also has a per-prototype block:

- **Precise Place Settings** — contains [Position Offset](/megaworld/shared-settings/position-offset) for shifting each prototype's placement point individually.

## Mouse actions

The current mouse-action stack includes:

- **Move Along Direction**
- **Rotation**
- **Scale**

## Shared systems used by this tool

Precise Place uses:

- `Success`
- [Transform Components](/megaworld/transform-components/overview)
- [Overlap Check Settings](/megaworld/overlap-check-settings/overview)

## When to use Precise Place

Precise Place is best when you want:

- careful one-by-one placement with a preview object;
- manual control with optional transform and overlap assistance;
- more precision than a brush workflow, but more convenience than placing prefabs completely by hand.

<Note>
  Use Precise Place when final art direction matters. It is especially useful for hero props, hand-authored paths, edge placements, and scene accents.
</Note>
