---
title: "Stamper Tool Controller"
description: "Main action and mode panel shared by stamper tools in MegaWorld"
---

Stamper Tool Controller is the main action and mode panel for stamper-based tools.

It controls visualization, auto respawn behavior, and the core spawn actions.

## Settings

**Enable Visualization** — shows the Mask Filter output directly in the scene view. Lets you see exactly which areas pass your height, slope, and noise filters before spawning. Visualization appearance is configured in [Preferences](/megaworld/preferences/overview).

**Enable Auto Spawn** — automatically removes and re-spawns the current pass every time you change a setting. This gives you instant live feedback while tuning filters and scatter rules.

- **Delay Auto Spawn** — how many seconds to wait after a setting change before triggering the automatic respawn. Increase this if your area is large and you don't want a respawn on every small adjustment.

**Respawn** — removes all currently spawned instances from this stamper and runs a fresh spawn pass. Use this to regenerate the result after making manual changes.

**Spawn** — adds a new spawn pass on top of whatever is already in the scene. Each click of Spawn adds more instances without clearing the previous result.

**Unspawn Selected Prototypes** — removes only the instances that belong to the currently selected prototypes, leaving everything else untouched. Useful for clearing a specific resource type without redoing the whole stamper.

## Where it is used

- [Terrain Spawner](/megaworld/tools/terrain-spawner)
- [Gravity Spawner](/megaworld/tools/gravity-spawner)
