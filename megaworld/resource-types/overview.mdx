---
title: "Types and Prototypes"
description: "How MegaWorld organizes resources into Types and Prototypes for placement and painting"
---

MegaWorld organizes everything you place or paint through two concepts: **Types** and **Prototypes**.

## Types

A Type is a named container stored as a project asset. It groups prototypes of the same kind, such as a set of pine trees, a collection of rocks, or a group of grass textures.

Types are reusable across tools. You create a Type once in your project and drag it into any tool's selection.

<img
  style={{ borderRadius: '0.5rem', width: '100%', maxWidth: '544px' }}
  src="/images/megaworld/window-workflow/types-folder.png"
  alt="MegaWorld types folder in the project."
/>

Each Type holds one of MegaWorld's resource kinds:

- **Unity/GameObject** — standard Unity prefab objects
- **Terrain Object** — objects managed through a custom renderer
- **Unity/Terrain Tree** — tree instances stored in Unity Terrain tree data
- **Unity/Terrain Detail** — grass and mesh details on Unity Terrain
- **Unity/Terrain Texture** — texture layers on Unity Terrain

A Type can only hold one resource kind. You cannot mix GameObjects and Terrain Textures in the same Type.

## Prototypes

A Prototype is a single resource entry inside a Type. It wraps one asset — a prefab, a terrain layer, or a detail definition — and holds any prototype-level settings on top of it.

Each Prototype has:

- a unique **ID** used by MegaWorld to track it across tools;
- an **Active** toggle that disables the prototype without removing it;
- settings from the component stack, such as filter, transform, and overlap rules that apply to that prototype specifically.

## How they work together

You bring a Type into the tool's selection, and MegaWorld uses all active Prototypes inside it when spawning or painting. The tool works across all selected Types simultaneously, evaluating each Prototype according to its own rules.

<img
  style={{ borderRadius: '0.5rem', width: '100%', maxWidth: '904px' }}
  src="/images/megaworld/window-workflow/types-selection.png"
  alt="Dragging types into the MegaWorld selection."
/>

## Resource kinds

| Resource kind | What it places | Requires |
|---|---|---|
| Unity/GameObject | Any prefab | — |
| Terrain Object | Prefab via custom renderer | Terrain Object renderer in scene |
| Unity/Terrain Tree | Tree prefab | Unity Terrain in scene |
| Unity/Terrain Detail | Grass or mesh detail | Unity Terrain in scene |
| Unity/Terrain Texture | Terrain layer / texture | Unity Terrain in scene |

See the individual pages for details on each resource kind.

## Resources Controller actions

The prototype inspector shows a **Resources Controller** section for resource types that need scene data, terrain data, or renderer data to stay in sync.

| Resource kind | Controller actions | When to use them |
|---|---|---|
| Unity/GameObject | Refresh Cells | After moving placed GameObjects manually outside MegaWorld |
| Terrain Object | Get Resources | To copy renderer prototypes from Terrain Object Renderer into the MegaWorld Type |
| Unity/Terrain Tree | Get/Update Resources From Terrain, Add Missing Resources, Remove All Resources, Refresh Cells | To sync tree prototypes with Unity Terrain and refresh tree spatial cells |
| Unity/Terrain Detail | Get/Update Resources From Terrain, Add Missing Resources, Remove All Resources | To sync detail prototypes with Unity Terrain detail prototypes |
| Unity/Terrain Texture | Get/Update Resources From Terrain, Add Missing Resources, Remove All Resources | To sync texture prototypes with Unity Terrain layers |

<Note>
  Terrain-backed resource types depend on the active Unity Terrain. If no active terrain exists, MegaWorld shows a warning instead of the terrain sync actions.
</Note>
